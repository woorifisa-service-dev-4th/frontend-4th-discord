import React from 'react';

const SectionOneAndFour = ({children}) => {
    return (
        <div className={"flex flex-col flex-1 bg-discord1and4"}>
            {children}
        </div>
    );
};

export default SectionOneAndFour;